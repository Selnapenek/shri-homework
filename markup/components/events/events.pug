include ../card/card.pug

// data - array of events (cards)
mixin events(data)
    main.events
        div.events__title
            h1 Лента событий
        .card__container
            - let events = data;

            each val in events
                +card(val)
            else 
                div There are no values
